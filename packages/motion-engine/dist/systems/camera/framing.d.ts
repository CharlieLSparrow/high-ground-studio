import * as THREE from "three";
export declare function createIntroCamera(aspect: number): THREE.PerspectiveCamera;
export declare function frameIntroCamera(camera: THREE.PerspectiveCamera, width: number, height: number): void;
