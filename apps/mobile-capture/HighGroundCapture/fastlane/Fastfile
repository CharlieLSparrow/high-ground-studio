default_platform(:ios)

platform :ios do
  desc "Push a new beta build to TestFlight"
  lane :beta do
    increment_build_number(xcodeproj: "HighGroundCapture.xcodeproj")
    build_app(workspace: "HighGroundCapture.xcworkspace", scheme: "HighGroundCapture")
    upload_to_testflight
  end
end
