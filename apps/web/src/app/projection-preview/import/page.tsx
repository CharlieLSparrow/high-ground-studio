import ImportProjectionPreviewClient from "./ImportProjectionPreviewClient";

export const metadata = {
  title: "Import Projection Preview | High Ground Odyssey",
  description:
    "Browser-only High Ground Odyssey projection JSON import preview for Studio handoff drafts.",
};

export default function ImportProjectionPreviewPage() {
  return <ImportProjectionPreviewClient />;
}
