import ArtifactProjectionStageClient from "./ArtifactProjectionStageClient";

export const metadata = {
  title: "Staged Artifact Inspection | High Ground Odyssey",
  description:
    "Browser-only staged projection artifact contract validation and inspection.",
};

export default function ProjectionStageArtifactPage() {
  return <ArtifactProjectionStageClient />;
}
