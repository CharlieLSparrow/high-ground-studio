# Example Completion

This example completes a word guessing puzzle with hints.

## How To Run

```bash
python3 main.py
```

## What It Demonstrates

- a `while` loop;
- string length validation;
- iterating over letters with `enumerate`;
- conditional logic inside a loop;
- counting guesses.
