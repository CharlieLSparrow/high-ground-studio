# Retained native project operation

Date: 2026-07-31

Exact source `5538d01c6286c0c96a5f11c79d5248101491ee19` proves that the
shipping Quipsly Capture iPhone shell can create and continue useful canonical
work through the real compiled app. The journey uses the fixed retained
media-production `.test` identity and keeps every product artifact for
longitudinal regression testing.

## Defect found by doing real work

The first project was created and selected in Work, but Quick Task resolved its
destination through the Session client's older project catalog. The visible
sheet therefore fell back to an unavailable Session or unfiled Home Nest even
though Work already held the authoritative project.

The correction establishes one clear ownership boundary:

- Work projects the selected canonical project and its active tag vocabulary
  from the current Work workspace;
- quick entry preserves that explicit project even when Session's catalog has
  not refreshed yet;
- the sheet offers the exact project rather than silently changing the
  destination; and
- Task, Note, and Goal reuse the same canonical project and tag identity.

The permanent mobile source contract now pins this boundary.

## Retained regression history

The failed attempts were intentionally preserved rather than cleaned up:

- `QA Retained · iPhone project organization 2026-07-31 542c0863` proves the
  original stale-destination failure;
- the `B` project reached the corrected destination and exposed a keyboard
  focus problem in the UI test;
- the `C` project saved the canonical Task and exposed XCTest's 128-character
  identifier limit; and
- the `D` project completed Project, Task, Note, Goal, and shared-tag creation
  from the working tree.

These are private synthetic fixtures, not abandoned customer data. They remain
useful for search, deduplication, archive UX, migration, and regression work.

## Exact committed operation

The clean committed source created:

- project `QA Retained · Native work 5538d01c E`;
- task `QA Retained · Organize work 5538d01c E`;
- private document-kernel note derived from the Task title;
- active Goal derived from the Task title; and
- shared tag `QA Retained · Longitudinal QA` on all three records.

The compiled iPhone 17 Pro simulator journey passed 1/1 in 112.644 seconds.
Independent canonical readback then proved:

- one private project and one active OWNER grant;
- one OPEN self-owned Task with no invented Session or due date;
- one private personal Note with two blocks and one document operation;
- one ACTIVE self-owned Goal with no invented Session or target date;
- the same active tag identity on Task, Note, and Goal;
- `humanCommitted: true` and `externalSideEffects: false` on every source
  contract; and
- preserved artifacts with no credential output.

The redacted identity hashes are:

- project: `4ae7df375a9bf7b25599edd3d533bb9f1ecc9b39d569072982a473e08e199a8a`
- task: `1b4390e5a9f213cf47983865aa279257cc75b14751ea350c862f89ebe66f8f5f`
- note: `f9b9e53a40b9793cca8d2793f2f8825524c285adf34812f00ffb3bc946f06c8a`
- goal: `bb03b5907285954ad695756b092f971055fdabc1186e21fe13e4f5fecb7edf69`
- tag: `fcd63c621f1ce6d28996046e4c18fe75d5ac61c3f6d91b6804b5f1ae03d9501e`

The preserved result bundle is
`/private/tmp/quipsly-retained-native-project-1785518752275-97752.xcresult`.

## Repeatable operator lane

Run the operation only against loopback Nest, local Firebase Auth, and local
PostgreSQL. Supply unique, visibly retained project and Task names:

```bash
pnpm quipsly:retained:native-project -- \
  --project "QA Retained · Native work <source> <run>" \
  --task "QA Retained · Organize work <source> <run>" \
  --tag "QA Retained · Longitudinal QA"
```

The operator reads the fixed password from the dedicated macOS Keychain item,
passes it only through the test process environment, refuses remote origins or
databases, refuses an existing exact project name, emits hashes rather than
secrets, and contains no product-artifact cleanup path.

## Verification

- Exact committed compiled iPhone operation: 1 passed, 0 failed, 0 skipped.
- Retained-operation boundary tests: 3/3 passed.
- Quipsly TypeScript and optimized 150-route production build: passed.
- Media processor typecheck and bundled build: passed.
- TypeScript 7 repository gate: all 27 tracked projects passed on 7.0.2.
- Shell, JavaScript syntax, and explicit diff checks: passed.
- Mobile source-contract delta: no regression; the same eight unrelated
  failures occur on clean parent source and remain tracked baseline debt.

This is exact-commit local compiled-app and canonical-database evidence. It is
not a production deployment, TestFlight installation, physical-iPhone capture,
recording/upload recovery, real episode workflow, or real coaching workflow.
