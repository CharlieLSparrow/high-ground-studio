# Quipsly deferred validation ledger

Updated: 2026-08-25

This ledger keeps human, physical-device, live-service, and environment-bound
acceptance work visible without blocking safe independent development. An item
leaves this ledger only when the named evidence exists. Automated checks must
never be substituted for the evidence requested here.

## Capture and coaching flight

| State | Validation still required | Evidence that closes it |
| --- | --- | --- |
| Pending | A minimally instructed coach and client complete scheduling, invitation, lobby, permissions, consent, join, conversation, participant-owned recording, leave, upload, and playable Session return without developer intervention. | Timestamped two-account flight record, both participant perspectives, retained recording/source IDs, post-call playback, and observed friction. |
| Pending | The current Capture release runs the recording and recovery path on physical iPhones across interruption, background/foreground, temporary network loss, exhausted automatic reconnect, one-tap manual rejoin, and a genuine CallKit/headset hang-up. The call drop must not stop the participant-owned master; the deliberate system hang-up must protect it. | Device/build identities; before/drop/rejoin/leave timestamps; visible Rejoin state; source and segment receipts spanning the gap; retained `call-transport-gap` clock evidence; upload reconciliation; exact-byte verification; playable beginning/gap/rejoined/ending readback; and listening evidence that describes the actual local source instead of inferring silence from network loss. |
| Pending | Fresh coach/client identities see only Home plus explicitly shared Nests, Sessions, notes, tasks, goals, transcripts, and media. | Two-account positive/negative UI checks plus direct authenticated route probes. |
| Pending — browser local-vault ownership automated | On one shared Safari/Chrome profile, record and interrupt a protected local source as the coach, sign out, sign in as the client who also belongs to the same Session, and reopen it. Confirm the client cannot see, download, retry, finalize, upload, or publish endpoint-queue state for the coach's OPFS source. Return to the coach and confirm the exact ledger and bytes remain recoverable. Repeat with a legacy pre-v2 vault upgrade, offline relaunch, and an unrelated outsider. | Browser/profile/account/participant/room/capture identities; IndexedDB v1-to-v2 upgrade observation; coach positive and client/outsider negative recovery UI; no client network attempt containing the coach participant/capture IDs; participant-scoped endpoint cursor keys; coach retry/upload/finalization receipts; exact-byte hash and beginning/middle/ending playback. The six focused suites (27 tests), compound-index source inspection, TypeScript, and server actor-owned upload checks do not prove real browser upgrade behavior or human account switching. |
| Pending — browser vault upgrade and commit liveness automated | Hold the v1 recording vault open in one real Safari/Chrome tab while a second current Quipsly tab requests v2. Confirm a current connection releases on `versionchange`; a truly legacy blocker gets a visible, retryable readiness failure instead of an infinite spinner; closing it and retrying restores the vault with every source unchanged. Force a ledger transaction to abort after request success and confirm Capture never advances the source or chunk journal as durable. | Browser/version/tab/profile identities; open/blocked/versionchange/close/retry and transaction request/abort timestamps; before/after IndexedDB indexes and ledger rows; unchanged OPFS byte count and SHA-256; visible recovery message; successful retry; beginning/middle/ending playback; and proof no upload or source state advanced from the aborted write. The six focused suites (36 tests), full TypeScript, and production build prove lifecycle ownership and commit-bound resolution, not real multi-tab browser scheduling, forced storage failure, or media playback. |
| Pending — browser OPFS worker ambiguity automated | During a real retained audio source and then a video source, freeze or terminate the synchronous OPFS worker while a chunk flush is outstanding. Confirm Capture waits only the single bounded writer timeout, releases recording hardware, preserves its last acknowledged ledger size, enters visible Held recovery, and sends no further write or close request to the failed worker. Relaunch and reconcile the actual file length without inventing a committed chunk or duplicating the source. | Browser/OS/account/participant/capture/worker/OPFS identities; last acknowledged chunk and byte offset; timeout, termination, hardware-release, Held, relaunch, reconciliation, upload, and finalization timestamps; before/after ledger and file sizes; exact SHA-256; beginning/middle/ending playback; no second 60-second wait; no late worker command; and no duplicate RecordingAsset. The focused fake-worker behavior test proves terminal poisoning and immediate rejection, not real worker scheduling, flush atomicity, hardware release, or recovered playback. |
| Pending — browser write-ahead chunk recovery automated | Crash or terminate a real browser at each boundary of one middle chunk and the very first chunk: before intent commit, after intent/before OPFS, during OPFS write/flush, after flush/before acknowledgement, and after acknowledgement. Relaunch and confirm Quipsly selects only the exact acknowledged boundary or the exact full pending-chunk boundary, promotes a committed pending chunk once, discards only an uncommitted intent, and holds every partial/unexplained tail without changing the file. Exercise **Recover recording**, automatic recovery, STOP repair, upload, finalization, playback, and transcript start. | Browser/OS/account/participant/capture/OPFS/ledger identities; intent/request/flush/ack/crash/relaunch timestamps; before/after chunk journals and file sizes; recovery disposition; partial-tail negative upload proof; unchanged held-file bytes; exact SHA-256; one upload session and RecordingAsset; beginning/middle/ending playback; transcript job/source identity; and no truncation or duplicate chunk. The eight focused suites (41 tests) and shared/app TypeScript prove write ordering and deterministic reconciliation, not real crash scheduling, OPFS durability, upload, playback, or transcription. |
| Pending — resumed browser upload local-proof automated | Finish and hash a real long source, begin resumable upload, close the browser, then relaunch and retry. Confirm Quipsly rehashes the exact OPFS file before its first reservation/status request and resumes normally. On a disposable copy, mutate one byte without changing length and separately change length; both must become Held before any network request, remain downloadable byte-for-byte, and require an explicit repaired source rather than silently overwriting the ledger checksum. | Browser/account/participant/capture/upload/OPFS identities; initial and retry hash/size; network trace proving no reservation/status request precedes retry proof; mutation method and negative request evidence; Held UI; downloaded mutated-file hash; repaired-source identity; idempotent upload/finalization; canonical RecordingAsset hash; and beginning/middle/ending playback. The eight focused suites (43 tests), helper proof cases, source-order contract, and TypeScript prove the pre-network boundary and immediate-proof reuse, not real large-file hashing, mutation, browser network order, or playback. |
| Pending — explicit native decline UI automated | A first-time and returning participant traverse the conventional outer room on browser and physical iPhone: contextual device grant, visible mic activity, selected-output speaker test, one Join, Session recording choice, Record, leave, and return. Explicitly choose **Don't record me**, confirm the call remains usable while no participant source starts, relaunch and verify readback, then deliberately change to **Allow recording**. Repeat with permission denied, camera unavailable, and an iPhone companion join. | Screen recordings from both endpoints; consent receipt and readback; observed prompt count; correct routed speaker-test output; remembered re-entry with no new prompt; call continuity before and after decline; no declined-participant endpoint/source receipt; ordinary Settings recovery; audio call survival after camera failure; no companion echo; later granted recording receipt IDs; and playable retained sources. The operated Simulator decline path does not prove live provider media, server persistence, or physical-device behavior. |
| Pending — bounded browser join permissions automated | In current Safari and Chrome, use a fresh site-permission profile to join a real two-person Session unmuted, then return with the retained grant. Confirm the first Join is the single contextual microphone/camera prompt boundary, the returning Join opens no new prompt, and an intentionally muted first Join asks for no microphone until Unmute. Repeat with camera busy or denied and confirm Quipsly retries microphone-only, joins camera-off, and preserves the conversation. | Browser/version/account/room identities; prompt timing and count; first and returning Join observations; deliberately muted negative prompt proof followed by Unmute grant; provider participant and track state; both participants' audibility; camera-failure message and microphone-only survival; and successful recovery from ordinary browser site settings. The 36-test room suite and TypeScript/contracts prove policy and degradation paths, not actual browser prompts or provider media. |
| Pending — live browser companion handoff automated | During a real browser/iPhone call, change the connected browser from **Audio on this device** to **Audio on another device**, then return. Confirm the provider microphone and every remote speaker element actually turn off before the UI claims companion mode, returning restores listening while remaining muted, and Unmute uses the remembered exact microphone. Force one provider-mute failure and confirm Quipsly keeps the original truthful mode instead of displaying a silent endpoint. Repeat while a browser-owned retained source is active and prove that only call transport changes. | Browser/account/room/endpoint identities; LiveKit publication and remote-element state; both participants' audibility and echo observations; displayed mode before and after each action; exact microphone device ID on return; failure rollback; and continuous retained source/clock/upload/playback evidence across the handoff. The 36-test room suite proves transport calls, UI rollback, and DOM speaker state, not real provider audibility or retained-source independence. |
| Pending — join-muted UI automated | With the exact LiveKit 2.16.0 Capture build, join a real two-participant provider room from a physical iPhone with call audio on and microphone off; hear the other participant without a microphone prompt, then tap Unmute and grant access. Start a participant-owned master, alternate Mute and Unmute during speech, and verify that Mute silences only the outbound call while the exact local master keeps PCM, clock, and level continuity. Repeat with denial/Settings recovery, rejoin, Bluetooth or wired audio, and a second-device companion. | Build, exact LiveKit/WebRTC versions, device and room identities; both participant observations of the mute state; remote-audio audibility before local unmute; prompt timing and count; routed listening/speaking notes; remembered re-entry; companion echo check; continuous local level/segment/clock/source receipts across every mute transition; other-participant proof of outbound silence; and playable post-call source readback containing the locally spoken muted intervals. The operated Simulator lobby, dependency upgrade, source inspection, PCM watchdog, and static publication/subscription contracts do not prove real provider media, audibility, or retained muted speech. |
| Pending — pre-join camera UI automated | On a physical iPhone, turn Camera on in the outer room, grant access, inspect and switch the real front/back preview, join a real two-participant call, change camera state in-call, rejoin, and run a simultaneous participant-owned 4K/24 source. Open and dismiss Control Center plus a system alert and verify temporary loss of focus does not end the movie. Then actually background/lock the app and verify the movie closes under a background task, becomes playable, and the call/audio behavior stays honest. Repeat with a deliberately paused capture group and confirm foreground return preserves Paused plus explicit Resume. Also cover denial, Settings recovery, rotation, tab departure, and camera failure while audio survives. | Build/device/room identities; prompt timing and count; both participant views; preview and remote framing observations; temporary-overlay continuity; background-task/finalization timestamps; paused-group state before/after background; camera-indicator shutdown after leaving the unjoined lobby; provider track state; retained source/segment/profile/upload receipts; exact-source playback; and proof that one AVFoundation owner served preview, call video, and the unchanged local master. Simulator UI, lifecycle source contracts, and static ownership contracts do not prove camera hardware, scheduling time, movie finalization, or media transport. |
| Pending — purpose-specific native recording defaults automated | On a physical iPhone, select a coaching Session and confirm Audio is the first-use default; choose another coaching mode, switch to a podcast Session, and confirm the podcast independently defaults to Podcast A/V. Set distinct choices for both purposes, relaunch, switch between them, and confirm each choice returns without changing modes during an active capture. Complete real requested/granted consent and a short retained recording in each purpose. | Build/device/Session identities; before/after purpose and selected-mode observations; relaunch readback; proof that an active capture was not repurposed; permission and consent receipts; podcast microphone-master plus camera-only source receipts in one capture group; coaching audio source receipt; upload verification; and beginning/middle/ending playback. Static preference contracts and Simulator compilation do not prove hardware capture or human comprehension. |
| Pending — call-route UI automated | On physical iPhones, join one real two-participant call and change among receiver, speaker, wired headphones, Bluetooth HFP, and supported USB audio before and during the call; remove a route during conversation and during participant-owned recording, then reconnect. Confirm Speaker remains usable during a protected master, plainly warns about call bleed, and does not stop or replace the source. Repeat in companion mode. | Build/device/room identities; before/after displayed input and output names; both participants' routed listening/speaking notes; Speaker state and recording-continuity message; Bluetooth profile; reconnect and echo observations; continuous local PCM/segment/clock/upload receipts spanning each change; exact-source playback including a quality comparison of speaker versus headphones; and confirmation that companion mode offered no local route ownership. Simulator route UI, compilation, and source contracts do not prove hardware selection, audibility, retained PCM, or echo behavior. |
| Pending — reconnect policy and choices automated | During a real physical-iPhone participant-owned recording, recover one transient provider reconnect and exhaust another into manual Rejoin. Speak continuously through the exhausted reconnect while both unmuted and muted, and verify the provider-input retention lease keeps the protected PCM path alive while CallKit deactivates and restarts. Before tapping Rejoin, navigate to a different Session and confirm it still shows ordinary Join and cannot borrow the disconnected room's lock bypass; return to the original Session and Rejoin. On browser and iPhone, deliberately mute and stop camera before another exhausted reconnect; confirm the Rejoin lobby visibly preserves both choices and the provider reconnects with neither source unexpectedly live. Repeat with CallKit reset, system/headset hang-up, backgrounding, an already-published camera backed by an active 4K local master, Bluetooth, companion mode, and a server-closed Session. | Build/device/browser/room identities; SDK and CallKit event order; visible Reconnecting/Rejoin/closed states scoped to the exact Session; cross-Session negative proof; before/after mic, camera, and route choices; provider publication proof that deliberate mute/camera-off survives manual Rejoin; remote-participant proof that an intentionally active camera restores live frames from the still-running local source; continuous local level/segment/clock/upload receipts across the transient and exhausted gaps; beginning/gap/rejoined/ending listening and viewing including speech captured while room transport was absent; proof that exhausted provider cleanup kept the master while a deliberate hang-up protected and stopped it; proof that a CallKit reset offers recovery only for its affected room; and proof that a closed Session rejected Rejoin. The 37-test browser room suite, Simulator compilation, retained-engine and room-scoping source contracts, preference relaunch, and static publication/end-disposition contracts do not prove physical scheduling, remote video, audibility, or source continuity. |
| Pending — coordinated waiting/retry UI automated | In a real two-account Session, let the coach control room recording while the client completes consent and device access. Confirm the client sees Ready rather than a disabled Record button, both endpoints honor one durable START, an induced local start failure retries against that command, and one Stop protects both sources. Repeat with browser/iPhone, late join, permission denial/recovery, an authorized Nest owner/editor, and a temporarily offline endpoint. | Actor/participant/endpoint/directive/receipt/capture/finalization/RecordingAsset identities; both perspectives; visible state transitions; proof that retry did not create a second START; exact-source upload and beginning/middle/ending playback; and observed comprehension. The operated Simulator status and server authority tests do not prove cross-device media or human understanding. |
| Pending — coordinated receipt outbox automated | During a real two-account recording, interrupt one iPhone before its STARTED receipt reaches Nest, force-quit after local capture begins, relaunch offline, restore the same account and network, and repeat across STOPPING and STOPPED. Switch accounts while one receipt is pending. | Original account/room/directive/receipt/capture identities before and after relaunch; idempotent server replay; no duplicate room START; Nest endpoint-state convergence; second-account negative visibility/delivery proof; exact-source upload and playback. The operated protected-store relaunch/account-switch test does not prove real network recovery or media. |
| Pending — browser coordinated receipt recovery automated | During a real two-account browser/iPhone recording, take the browser offline after local STARTED and again after STOPPED, close and reopen the tab, restore the same signed-in participant, and then switch the browser to another account before restoring network. | Original participant/room/directive/receipt/capture identities; visible saved-status recovery message; exact request replay; single-flight network evidence; Nest endpoint convergence; other-account negative delivery proof; retained browser-source upload, verification, and beginning/middle/ending playback. Unit account-partition/replay evidence does not prove browser storage policy, real navigation, network recovery, or media. |
| Pending — dock navigation safe-leave automated | Start a real retained browser source, minimize the call, request a second Session, and choose **Leave & switch**. Repeat with **Leave & close**, a disconnected call whose local source is still active, an offline finalization retry, an induced OPFS chunk-write failure, no active recording, and exit while provider Join is deliberately delayed. Confirm the old room stays mounted until its recorder reports protected completion, then exactly one requested destination opens or the dock closes; neither a write error nor the delayed Join may release or resurrect provider/media state before finalization owns the result. | Browser/account/old-room/new-room/capture identities; visible protecting state; source STOP/finalization/upload receipts; writer-error and `onstop` order; mount/unmount timestamps showing safe-leave acknowledgement precedes unmount; provider connection/media state after delayed completion; correct destination; offline recovery; exact-source beginning/middle/ending playback; and no duplicate stop or lost original. The 41-test room/dock suite plus focused failure-path source contract prove the shell handshake, async cancellation, and status ownership, not MediaRecorder/OPFS finalization, upload, or real navigation timing. |
| Pending — concurrent browser recovery ownership automated | Start one protected browser source while an older source for the same participant is waiting locally. Trigger `online` and visible-tab recovery during start, recording, stop, hashing, and active upload; repeat with an interrupted source, a transient held upload, and a coordinated START arriving during recovery. Confirm the current source keeps its own capture ID, OPFS file, chunks, status, STOP receipt, and upload session while the older source resumes only after ownership is released. | Browser/account/participant/room identities; both capture, upload-session, OPFS, START, and STOP identities; event ordering across each trigger; distinct IndexedDB rows and endpoint snapshots; no chunk-offset or file cross-write; both exact SHA-256 values; beginning/middle/ending playback of each source; and coordinated endpoint convergence. The seven focused suites (30 tests), active-ID source contract, and TypeScript prove the intended isolation and deferral, not real MediaRecorder, IndexedDB, OPFS, network-event, or provider scheduling. |
| Pending — browser STOP receipt repair automated | During ordinary stop, interruption recovery, and a completed upload, lose the durable Session STOP response before and after server commit. Confirm exact media upload and verification continue, the same immutable STOP identity remains locally pending, the endpoint queue never claims Drained early, manual/online/relaunch repair replays the same request, and room state converges without another START or duplicate media. Repeat after switching the shared browser to another account. | Browser/account/participant/room/capture/upload/STOP identities; request and response-loss timing; participant-scoped IndexedDB before/after state; queue revisions; server idempotent replay; room active-capture readback; other-account negative network and UI proof; exact SHA-256; beginning/middle/ending playback; and no duplicate RecordingAsset. The eight focused suites (38 tests), shared repair source contract, TypeScript, and Capture contract prove intended state separation and queue truth, not real network loss, browser relaunch, server commit ambiguity, or media playback. |
| Pending — browser writer-close hardware release automated | Induce a delayed and then rejected OPFS writer close after a real audio source and a real video source stop. Confirm microphone/camera indicators and provider preview ownership end immediately, the last committed chunks remain in the participant-owned vault, the failed close becomes visible Held recovery, relaunch finalizes the bounded interruption, and neither source is duplicated or silently reported complete before exact verification. | Browser/OS/device/account/participant/capture/OPFS identities; `onstop`, track-ended, writer-close, ledger, and indicator timestamps; camera/microphone indicator observation; IndexedDB/OPFS bytes before and after relaunch; recovered interruption profile; exact SHA-256 and size; upload/RecordingAsset identity; beginning/middle/ending playback; and no source deletion. The five focused suites (32 tests), cleanup-order source contract, and TypeScript prove code ownership order, not real browser hardware release, writer failure, or recovered playback. |
| Pending — canonical browser finalization identity automated | Finalize one real short audio source and one long 4K source while delaying the app-owned finalization record after exact-byte verification. Return repeated 202 responses and one deliberately malformed verified response without a `RecordingAsset` ID. Confirm the browser remains Verifying, retries without a duplicate upload, keeps its queue non-drained and local original retained, then becomes Verified only after the exact canonical asset is readable and playable. | Browser/account/participant/capture/upload/object-generation/finalization/RecordingAsset identities; 202 and malformed-response timing; bounded retry requests; before/after IndexedDB and endpoint-queue snapshots; exact SHA-256/size; single RecordingAsset and transcript job; beginning/middle/ending playback; and no premature review link or source deletion. The focused finalization/queue/recorder suites, TypeScript, and production build prove fail-closed projection and retry wiring, not real GCS verification latency, database convergence, browser timers, or playback. |
| Pending | A coach returns after the call and plays the beginning, middle, and ending of each retained participant source through the protected Session player; for multiple sources, inspect conversational sync and confirm the player is bound to the intended participant and take. | Session/source/RecordingAsset identities; observed authenticated playback on browser and iPhone; beginning/middle/ending listening or viewing notes; sync observation; and confirmation that exact originals remain unchanged. Automated route and complete-decode checks do not close this item. |
| Pending | A released real two-person recording starts transcription automatically without pressing Run, then a coach reviews it against protected playback: beginning/middle/ending word highlighting, participant attribution, one wording correction on each participant source, one speaker correction or confirmation, refresh, cross-device readback, and a reversible transcript-based trim preview. The browser must switch to each passage's exact source. On iPhone, first prove the wrong local original cannot authorize the passage, then prepare the other participant's verified protected copy and prove listened-position receipts unlock only that exact source. | RecordingAsset, finalization receipt, transcript job/outbox, worker execution, segment/word, correction/verification, speaker-attribution, and edit-proposal identities; observed source-clock alignment; exact player/source identities for both participants; wrong-source negative proof on iPhone; retained provider evidence; effective corrected readback; unchanged source bytes; and explicit proof that preview did not silently mutate the canonical timeline. |
| Pending — cloud Studio source path automated | Import or retain one real generation-bound GCS audio source and one video source in a coaching or Episode space, authorize transcription in product, let the shared cloud worker finish, and review beginning/middle/ending words against protected exact-source playback. Repeat one request to prove create-once behavior and deliberately replace a disposable source generation before registration to prove the stale result is rejected. | Studio processing job and canonical TranscriptJob IDs; manifest, queue, result, and raw-provider object names/generations; provider request ID; source and provider-response SHA-256/bytes; worker build/digest; segment/word and speaker coverage; protected listening notes; replay generations unchanged; stale-generation rejection; and unchanged original source. |
| Pending — camera derivative automated | From a released real 4K camera-only source with embedded audio, tap a transcript passage and a text-edit passage on physical iPhone. Quipsly must create/reuse one compact AAC derivative, play the exact beginning/middle/ending source-clock ranges, survive relaunch, and refuse a deliberately stale source/result binding without downloading the camera master. | RecordingAsset/finalization/job/manifest/result identities and generations; source and derivative SHA-256/byte counts; Cloud Run execution; observed downloaded byte size; audible range notes; relaunch/cache readback; stale-binding 409; unchanged camera source; and confirmation that no transcript correction, edit, share, or publication was implied by preparation. |
| Pending — browser and iPhone release gates automated | A coach creates a real private text-based edit, hears the rendered beginning, middle, ending, and both sides of every edit join on browser and physical iPhone, refreshes on another authenticated device, and releases it to the named client. Then change the edit, rerender, and prove the old listening review no longer authorizes Share. Client-observed playback is evidence of player coverage, not proof of attention or audibility. | Exact SessionOutput, revision, derivative RecordingAsset and SHA-256; required and observed second bins from each client; one `PLAYBACK_REVIEWED` revision bound to the derivative; cross-device readback; named-recipient delivery event; stale-review 409 after rerender; client positive and outsider negative playback; human notes about audible joins and routed output; unchanged participant masters; and no public link or external message. |
| Pending — private video edit and iPhone export automated | From one real two-person Session with a verified iPhone camera master and dedicated local audio, create a Video private preview on physical iPhone and browser, review the beginning/middle/ending and both sides of every transcript cut, export the exact reviewed copy through the iOS share sheet, download it in the browser, refresh on the other client, release it to the named client, and revoke it. Confirm a client has no export before release and an unrelated account never receives playback or export. Repeat once using embedded camera audio as the only audio for one participant. | Exact primary-camera and audio-program RecordingAsset IDs; v3 job/result and output generation; source/output SHA-256 and bytes; worker build; observed 1080p24 picture, routed sound, lip/speech sync, black missing-camera intervals, and edit joins; share-sheet file name/type/bytes plus cancel and completed outcomes; cross-client readback; recipient positive and outsider negative playback; release/revocation receipts; unchanged originals; and no public link. Local FFmpeg, decode, Simulator build, and UI tests do not close physical playback/export, Cloud Run, or human sync judgment. |
| Pending — reviewed placement automated | Two real participant-owned sources from one capture group assemble onto the Session program clock without erasing either source clock. Use Participant sync evidence to compare the provisional capture-clock offset with two exact-source FFT windows, approve the qualified reversible placement, protected-listen opening and late matches, then review transcript highlighting at beginning, middle, and ending. Queueing, exact take/byte binding, worker evidence, append-only approval/revocation, stale-binding rejection, and transcript/report consumption are automated; physical-device perception/readback remains to be collected. | Both source/transcript/capture-group identities; SessionAudioAlignmentJob, immutable cloud result, and decision receipt identities; result/request hashes and revisions; source generations; source-local plus measured program timestamps; opening/later offsets, correlation and peak margins; residual drift/ppm; protected playback observations; and proof that neither provider timing nor original bytes changed. |
| Pending | After real transcript completion, Quipsly prepares the coach's private summary, highlights, and task/goal/note candidates without Build packet or an open browser tab. The client cannot read that private packet, while the coach can explicitly accept one candidate into canonical work and share only a deliberately client-safe result. | Transcript/packet build IDs; canonical author identity; server reconciliation timing; coach positive and client/outsider negative UI plus direct-route checks; accepted-candidate receipt and resulting Work ID; unchanged private packet; and proof that no unreviewed candidate was assigned, shared, delivered, scheduled, edited, or published. |
| Pending — browser/iPhone round trip automated | A minimally instructed coach and client use the same Session Conversation from browser and physical iPhone before, during, and after a call; retry after a forced disconnect, reply, edit, remove, refresh, and distinguish Conversation from private/shared Notes and canonical Tasks/Goals. A clean outsider and a project-only viewer cannot read the thread by UI or direct route. The local retained flight now proves distinct fresh accounts, rendered browser-to-native-to-browser message readback, canonical actor binding, browser reply/edit/remove/retry/read-cursor behavior, and direct outsider-room denial; it does not substitute for physical-device or human comprehension evidence. | Two authorized account identities and two negative-control identities; room/message/revision/read-cursor IDs; browser and physical-iPhone screen observations; same request ID across the forced retry; cross-device readback; direct 404 probes; visible tombstone; unchanged notes/tasks/goals/transcript; and recorded comprehension friction. |
| Pending — relationship continuity automated | A minimally instructed coach and client use one coaching client space across multiple real Sessions on physical iPhones and browsers: exchange relationship-wide messages, reopen prior and upcoming Sessions, update shared work, create private notes from both accounts, force a disconnect/retry, and verify that Conversation, Session Conversation, Notes, Tasks, Goals, and recordings remain understandable and distinct. The retained fresh compiled-iPhone operation now proves coach-authored conversation persistence, invited-client readback, shared-work readback, coach-private-note filtering, exact Session entry, and unrelated-account `404` denial for conversation and work. | Physical device/build identities; coach/client screen observations across at least two Sessions; retry identity; routed notification or polling observations; both authors' private-note negative checks; exact Session returns; direct outsider probes; and recorded comprehension friction. |
| Pending — action-first client-space parity | A minimally instructed coach and client each open the same relationship from Nest and Quipsly Capture and independently identify the same next Session/action, open/overdue commitments, active goals, released prior follow-up, and their own private context. Nest and Capture now use the same versioned deterministic priority policy; role tests keep coach draft follow-up, client releases, and support/observer fallbacks distinct; the iPhone 17 Pro simulator proves the server-style projection resolves the exact Session and primary action. | Physical iPhone and browser observations from both roles; exact priority schema/rank plus engagement/room/task/goal/note/output IDs; matching live/late/upcoming/ended action; coach and client private-note negative checks; released-follow-up positive check; offline/cache fallback observation; and no horizontal overflow or ambiguous competing action at accessibility text sizes. |
| Environment retry | The local browser test harness reached the Quipsly server but timed out attaching its in-app webview during the 2026-08-24 preview-first lobby pass and the 2026-08-25 Capture fallback, live-lobby, and actionable client-portfolio passes. Focused DOM interaction, state-contract, deep-link, privacy-boundary, responsive-source, and production-build checks passed where recorded; those are not visual evidence. | A later local in-app browser run renders the ordinary coaching and podcast lobbies plus the `open=capture` recovery state, the coaching Today/Clients/Sessions portfolio, and one relationship-first client home at phone and desktop widths. Capture the one-action recording choice, join-without-recording path, device controls, one Join, and collapsed settings without overflow; then prove portfolio search/filters, role-bound New Session, live/follow-up/upcoming priorities, exact Session/client-space return, live/late/empty relationship actions, coach-private context, client negative visibility, status-aware Session history, and no client draft-follow-up signal. |

## Native audio mastery review

| State | Validation still required | Evidence that closes it |
| --- | --- | --- |
| Pending | On a physical iPhone, an authorized user downloads a real verified preview, hears every server-selected moment in original and improved versions, changes Fair comparison and Final volume during playback, and confirms the audible gain follows the selected mode without discontinuity. | Build/device identity, exact mastery job and derivative SHA-256, screen/audio observation notes, and no playback/download integrity error. |
| Pending | The same user completes one approval and one rejection against disposable mastery candidates and sees the append-only decisions read back in Capture and Nest without source replacement or implicit promotion. | Review receipt IDs, actor identity, exact source/preview hashes, cross-client readback, unchanged source bytes, and separate promotion state. |
| Pending | After approving a disposable preview on iPhone, the user selects it for delivery, confirms the same active candidate in Nest, withdraws it with a reason, and confirms the append-only history while no delivery encoding or publication is created. | Approval and promotion receipt IDs, exact active job/preview identity, cross-client readback before and after withdrawal, unchanged source bytes, and absent delivery/publication side effects. |
| Pending | On a physical iPhone, the user prepares a real share-ready AAC from a disposable active promotion, verifies the displayed encoding receipt, hears beginning, middle, and ending from the authenticated exact-byte download, records both an approval and rejection, and sees the append-only decisions in Capture and Nest. | Build/device identity; delivery job, promotion, and review receipt IDs; exact AAC SHA-256 and byte size; protected playback observation; cross-client readback; unchanged source/master bytes; and proof that no output packet, share, upload, or publication was created. |
| Pending | Promotion or mastery approval is withdrawn after an AAC has already been downloaded on iPhone; refreshing Capture must stop playback, remove local temporary authority, hold review, and require a newly authorized artifact path. | Before/after promotion and mastery receipt IDs, observed playback stop/held UI, temp-artifact lifecycle evidence, server 409 or held status for stale review, and successful recovery only after a current deliberate promotion. |

## Release evidence

| State | Validation still required | Evidence that closes it |
| --- | --- | --- |
| Environment retry — source ready | On 2026-08-25 the clean pushed source at `b3500af9` passed the complete local release-readiness source gate (`localSourceReady: true`, no local source blockers), the Quipsly production build, 24/24 coaching payment checks, 1,273/1,273 Capture/App Store checks, and the live public integration smoke. The selected `charlie@highgroundodyssey.com` gcloud user and Application Default Credentials could not mint tokens, so no preview deploy or promotion was attempted. Schema readback also remained a warning because no local `DATABASE_URL` was supplied. | Run `gcloud auth login --update-adc --brief`, then `gcloud auth application-default set-quota-project quipsly-reef`, capture a passing `bash scripts/release/quipsly-gcloud-auth-check.sh`, and rerun `node scripts/hgo-quipsly-release-readiness.mjs --json` before preview or promotion. |
| Pending | A deliberately batched Nest deployment and matching TestFlight build expose the same compatible mastery review plan and receipt contract. | Deployed commit/build IDs, authenticated live smoke, TestFlight processing status, and rollback coordinates. |

## Episode package review

| State | Validation still required | Evidence that closes it |
| --- | --- | --- |
| Pending | In an authenticated Session, select an approved Episode program, review its title/description/type/optional numbering and release intent, simulate one ambiguous response, refresh, and confirm exactly one new current packet while the predecessor and exact audio SHA remain visible in history. | Two packet IDs, two selection receipt IDs, identical artifact SHA-256, reviewed metadata readback, retained predecessor, stable request replay, and observed Session UX. |
| Pending | Attempt to select an approved artifact from a second Episode in the same Nest and attempt metadata save from a stale Session tab. | Both requests return their explicit 409 boundaries, no packet or receipt is written, and the legitimate current selection remains unchanged. |
