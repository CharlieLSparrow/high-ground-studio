# Now / Next / Later

Date: 2026-04-27

## Now

- Keep the repo memory/docs layer current.
- Verify meaningful web changes under both current build paths.
- Preserve the current guarded episodes loader unless there is a targeted replacement plan.
- Keep Stripe in the intentionally rolled-back state until build/content direction is clearer.

## Next

- Add `.env.example` from the actual env surface used in code.
- Keep the durable build-history note current if later sessions introduce new temporary failures or workarounds.
- Write a small content-pipeline map for:
  - `content/publish`
  - `content/_staging`
  - `content/_inbox`
- Add a narrow verification layer for auth and team server actions.

## Later

- Reintroduce commercialization only after the repo has a stable build and content path.
- Revisit the episodes route architecture after the loader-guard phase is no longer needed.
- Clean up backup/scratch artifacts after confirming they are not still serving as working notes.
- Decide whether the cart subsystem remains part of the product direction.
